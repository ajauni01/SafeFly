// place files you want to import through the `$lib` alias in this folder.
import NavBar from './components/NavBar.svelte'
import Footer from './components/Footer.svelte'
import { flightData } from './actions/flightData'

export {
    NavBar,
    Footer,
    flightData
}